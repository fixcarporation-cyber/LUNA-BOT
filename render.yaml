services:
  - type: web  # ✅ Allowed on free plan
    name: fix-corporation-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python LUNA.PY
    envVars:
      - key: DISCORD_TOKEN
        sync: false
