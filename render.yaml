services:
  - type: web
    name: luna-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python LUNA.PY
    envVars:
      - key: DISCORD_TOKEN
        sync: false
