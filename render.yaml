services:
  - type: web  # Change from 'worker' to 'web'
    name: fix-corporation-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python LUNA.PY
    envVars:
      - key: DISCORD_TOKEN
        sync: false
